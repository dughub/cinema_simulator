"""Running the simulation in the cinema environment."""

import simpy
import statistics
import random
import logging


class Cinema:
    def __init__(self, env):
        self.env = env
        self.cashiers = simpy.Resource(self.env, num_cashiers)


def optimise_wait_times():
    wait_times = []


if __name__ == '__main__':
    optimise_wait_times()
